# 評価テスト③ （L19〜L24）
## for/while・switch文・関数・WiFi・Bluetooth通信

**名前：＿＿＿＿＿＿＿＿　　日付：＿＿＿＿＿＿＿＿**

---

## 【筆記問題】

### 問1：プログラミングの基礎について (各2点 × 5問 = 10点)

次の説明が正しければ ○、間違っていれば × を書こう。

| 問 | 説明 | 答え |
|---|---|---|
| (1) | `for (int i = 0; i < 5; i++)` は5回繰り返す | ＿ |
| (2) | `while(true) { }` のブロックは永遠にループする | ＿ |
| (3) | switch文は float 型の変数にも使える | ＿ |
| (4) | `void` キーワードで戻り値のない関数を定義できる | ＿ |
| (5) | SoftwareSerial は D0・D1 ピンのみ使える | ＿ |

---

### 問2：空欄を埋めよう (各3点 × 4問 = 12点)

**(1)** for 文で変数 i を 0 から 9 まで繰り返すコードを書こう。

```C++
for (int i = ＿＿; i ＿＿ 10; i++) {
```

**(2)** 引数なし・戻り値なしの関数 `buzz_short` を定義するコードを書こう。

```C++
＿＿＿＿ buzz_short() {
```

**(3)** switch 文で変数 cmd が `'F'` のとき `go_Advance()` を呼ぶコードを書こう。

```C++
switch (cmd) {
  ＿＿＿＿ 'F':
    go_Advance(200, 1000);
    break;
}
```

**(4)** Bluetooth シリアル（BTSerial）にデータが届いているか確認するコードを書こう。

```C++
if (BTSerial.＿＿＿＿＿＿＿＿() > 0) {
```

---

### 問3：コードを読んで答えよう (各4点 × 3問 = 12点)

次のコードを見て問いに答えよう。

```C++
#define BUZZ_PIN 13

void buzz_times(int n) {
  for (int i = 0; i < n; i++) {
    digitalWrite(BUZZ_PIN, LOW);
    delay(200);
    digitalWrite(BUZZ_PIN, HIGH);
    delay(200);
  }
}

void loop() {
  buzz_times(3);
  delay(1000);
}
```

**(1)** `buzz_times(3)` を呼んだとき、ブザーは何回鳴りますか？

答え：＿＿＿＿＿回

**(2)** ブザーが1回鳴るのにかかる時間は何 ms ですか？

答え：＿＿＿＿＿ms

**(3)** `buzz_times` の引数 `n` を変えると何が変わりますか？

答え：
_______________________________________________

---

### 問4：コードを読んで答えよう (各4点 × 3問 = 12点)

次のコードを見て問いに答えよう。

```C++
#include <SoftwareSerial.h>
#include "motor_driver.h"

SoftwareSerial BTSerial(4, 5);

void setup() {
  init_GPIO();
  BTSerial.begin(9600);
}

void loop() {
  if (BTSerial.available() > 0) {
    char cmd = BTSerial.read();
    switch (cmd) {
      case 'F': go_Advance(200, 0); break;
      case 'B': go_Back(200, 0);    break;
      case 'L': go_Left(180, 0);    break;
      case 'R': go_Right(180, 0);   break;
      case 'S': stop_Stop();        break;
    }
  }
}
```

**(1)** Bluetooth モジュールの RX・TX はそれぞれ何番ピンに接続しますか？

答え：RX = ＿＿番、TX = ＿＿番

**(2)** スマホから `'L'` を送ったとき、ロボットはどう動きますか？

答え：＿＿＿＿＿＿

**(3)** `go_Advance(200, 0)` の第2引数が `0` のとき、ロボットはどう動きますか？

答え：
_______________________________________________

---

## 【実技問題】(54点)

### 実技1：関数とfor文でブザーを鳴らそう (18点)

引数で鳴らす回数を指定できる関数 `buzz_times(int n)` を作って、次の動作を実装しよう。

- 起動時：1回鳴る
- 3秒ごと：2回鳴る（while + delay を使う）

- [ ] `buzz_times()` 関数が正しく書けた（9点）
- [ ] 起動・繰り返しの動作が正しく動いた（9点）

---

### 実技2：switch文でロボットを Bluetooth 操縦しよう (18点)

Bluetooth モジュール（HC-06、RX=D4・TX=D5）を使い、スマホから文字コマンドを受け取って switch 文でロボットを操縦するプログラムを書こう。

| 送るコマンド | ロボットの動作 |
|---|---|
| F | 前進（スピード200） |
| B | 後退（スピード200） |
| L | 左折（スピード180） |
| R | 右折（スピード180） |
| S | 停止 |

- [ ] switch 文でコマンド判定が書けた（9点）
- [ ] 5種類の動作がすべて動いた（9点）

---

### 実技3：短いプログラムを作ろう (18点)

次の仕様を満たすプログラムを自分で書こう。

**仕様：**
- Bluetooth（RX=D4、TX=D5）でコマンドを受け取る
- コマンド `'1'`〜`'3'` で、ブザーを 1〜3 回鳴らす
- コマンド `'S'` でロボットを止める

ヒント：`buzz_times()` 関数と switch 文を組み合わせよう。

- [ ] `buzz_times()` 関数を使えた（6点）
- [ ] switch 文でコマンドを正しく判定できた（6点）
- [ ] 実機で正しく動いた（6点）

---

## 採点表

| 問題 | 満点 | 得点 |
|---|---|---|
| 問1 | 10 | ＿＿ |
| 問2 | 12 | ＿＿ |
| 問3 | 12 | ＿＿ |
| 問4 | 12 | ＿＿ |
| 実技1 | 18 | ＿＿ |
| 実技2 | 18 | ＿＿ |
| 実技3 | 18 | ＿＿ |
| **合計** | **100** | ＿＿ |

---

## 答え合わせ（講師用）

<details>
<summary>▼ 答えを見る</summary>

**問1：** (1)○ (2)○ (3)×（整数型・char型のみ）(4)○ (5)×（任意のデジタルピンが使える）

**問2：**
(1) `for (int i = 0; i < 10; i++) {`
(2) `void buzz_short() {`
(3) `case 'F':`
(4) `if (BTSerial.available() > 0) {`

**問3：**
(1) 3回
(2) 400ms（LOW 200ms + HIGH 200ms）
(3) ブザーが鳴る回数が変わる

**問4：**
(1) RX = 4番、TX = 5番
(2) 左折する（go_Left）
(3) 停止せずに動き続ける（time=0 のため delay がかからない）

</details>
